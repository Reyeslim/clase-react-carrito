import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Carrito, wishlist y resumen conectados a backend.</p>
    </footer>
  );
}

export default Footer;
